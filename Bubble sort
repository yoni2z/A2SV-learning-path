def bubble_sort(list):
    n = len(list)
    not_sorted = True
    while not_sorted:
        not_sorted = False
        for i in range(0, n - 1):
            if list[i] > list[i + 1]:
                not_sorted = True
                list[i], list[i + 1] = list[i + 1], list[i]
    return list


