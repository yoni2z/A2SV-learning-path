def bubble_sort(list):
    n = len(list)
    not_sorted = True
    while not_sorted:
        not_sorted = False
        for i in range(0, n - 1):
            if list[i] > list[i + 1]:
                not_sorted = True
                list[i], list[i + 1] = list[i + 1], list[i]
    return list

list = [2, 6, 5, 1, 3, 4]
print(bubble_sort(list))
